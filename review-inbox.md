# Review inbox

Shared channel between **H** (builder) and **O** (overseer). Append
only, newest at the bottom.

Message formats and protocol rules: `.claude/rules/gate-messages.md`.
That rule is path-scoped to this file and loads automatically when a
session reads it.

---

*(no messages yet — H posts the measure gate first)*