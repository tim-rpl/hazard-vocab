# DELIBERATELY VIOLATING — byte-identical to a clean file but for one
# line. `default_prefix: irwin` made the self-reference exemption treat a
# jurisdiction namespace as the schema's own, reopening the c1
# counterexample. Recall failure, introduced by the fix for a precision
# failure. See [O -> H] plan gate block verification 3, BV14.
id: https://w3id.org/hazard-vocab/core
name: lint_fixture_default_prefix_escape
default_prefix: irwin
prefixes:
  linkml: https://w3id.org/linkml/
  hv:     https://w3id.org/hazard-vocab/core/
  irwin:  https://w3id.org/nwcg/irwin/
default_range: string
imports: [linkml:types]
slots:
  incidentIdentifier:
    description: Identifier issued for an incident under some scheme.
    examples:
      - value: "2026-OR-ABC-000123"
    slot_uri: irwin:IrwinID