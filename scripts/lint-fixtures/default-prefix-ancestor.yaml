# DELIBERATELY VIOLATING — id: claims a foreign namespace AND
# default_prefix names an ancestor of it. The agreement test matched in
# both directions, so the ancestor was added to the self set. This is
# the ORDINARY shape a real schema has; the previously-shipped fixture
# fired only because it omitted default_prefix, so recall was
# demonstrated against the unusual shape.
# See [O -> H] plan gate block verification 5, BV23.
id: https://w3id.org/nwcg/irwin/core
name: lint_fixture_default_prefix_ancestor
default_prefix: irwin
prefixes:
  linkml: https://w3id.org/linkml/
  irwin:  https://w3id.org/nwcg/irwin/
default_range: string
imports: [linkml:types]
slots:
  incidentIdentifier:
    description: Identifier issued for an incident under some scheme.
    examples:
      - value: "2026-OR-ABC-000123"
    slot_uri: irwin:IrwinID