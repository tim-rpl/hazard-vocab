# DELIBERATELY VIOLATING — recall direction

A tracked file, outside every exclusion, containing a phrase the sweep
must report:

this exact string was retracted and must not return

Bare on purpose. A retraction cue in the sentence would exempt it, which
is the same reason several `guard-fixtures/` files are bare.