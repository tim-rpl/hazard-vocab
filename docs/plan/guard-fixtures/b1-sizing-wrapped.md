DELIBERATELY VIOLATING — B1/B3. A sizing claim, asserted and wrapped.

Note that the phrase must NOT be quoted here: `backticks` and
*'asterisk quotes'* are mention rather than use and are exempt by design,
so a quoted fixture tests the exemption instead of the match. The first
version of this file quoted it and passed, and the fixture matrix caught
that on its first run.

P5 is now wave 2. It is still the widest
item and everything in wave 3 waits on it.
