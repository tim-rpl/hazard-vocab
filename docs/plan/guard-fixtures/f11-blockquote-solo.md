MUST FIRE — the BLOCKQUOTE SOLO SPLIT alone must decide it.

O's residual on F11: "blockquote skip" was counted as one clause and is
two. The `continue` that skips a quoted unit is covered by
`retraction-blockquote-nocue.md`. The `solo` split that makes a `>` line
its OWN unit was not, and deleting it left all 18 fixtures identical to
control.

Below, the retraction is a blockquote with no terminal punctuation and
the reintroduction is the prose line immediately after it. With the
split they are two units and the prose one carries no cue, so it fires.
Without it they join, the cue reaches the figure, and the run stays
green — a widened exemption, which is B3's silent direction exactly.

> An earlier draft read all 23 external terms
The surface is 23 bind / 10 write of 33.
