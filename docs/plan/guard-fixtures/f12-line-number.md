DELIBERATELY VIOLATING — F12. The reported LINE NUMBER is the assertion.

The strip must sit in the SAME paragraph as the violation and before it,
or the offsets restart at the paragraph boundary and the bug cannot show.
The first version of this fixture put the mentions in an earlier
paragraph and passed with the fix reverted — it asserted a line number
without testing the mapping. `a backticked mention long enough to shift
the count by more than one line when it is deleted` and
*'an asterisk-quoted one, also long enough to matter for the offset'*
both vanish before matching, and then:
P5 resolves the 23 external identities.
