DELIBERATELY VIOLATING — F11. `re.I` on SIZING_PHRASES must decide it.

The capital is load-bearing and there is no lowercase alternative in the
line, which is the defect the first `b2-capitalised` fixture had.

**And no retraction cue may appear either.** The first version of this
file ended "...until 2026-08-05", and `until 2026` is a cue, so the
sentence exempted itself and the fixture tested nothing. Caught by the
matrix on its first run.

Widest Item is what P5 remains.
